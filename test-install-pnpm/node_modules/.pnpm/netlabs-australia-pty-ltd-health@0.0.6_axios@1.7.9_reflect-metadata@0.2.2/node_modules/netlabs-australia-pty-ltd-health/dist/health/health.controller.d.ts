import { ConfigService } from '@nestjs/config';
import { HealthCheckService, HttpHealthIndicator, TypeOrmHealthIndicator, MemoryHealthIndicator, DiskHealthIndicator } from '@nestjs/terminus';
import { PrometheusService } from './prometheus.service';
export declare class HealthController {
    private health;
    private http;
    private db;
    private memory;
    private disk;
    private readonly configService;
    private readonly prometheusService;
    private heapThreshold;
    private rssThreshold;
    constructor(health: HealthCheckService, http: HttpHealthIndicator, db: TypeOrmHealthIndicator, memory: MemoryHealthIndicator, disk: DiskHealthIndicator, configService: ConfigService, prometheusService: PrometheusService);
    checkLiveness(): Promise<import("@nestjs/terminus").HealthCheckResult>;
    checkReadiness(): Promise<import("@nestjs/terminus").HealthCheckResult>;
    private getExternalHealthChecks;
}
//# sourceMappingURL=health.controller.d.ts.map